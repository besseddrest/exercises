// Write a function that determines if a given number is odd.
// Follow up was to do it without using %, /, *, etc.

const isOdd = function(num) {
  return num % 2 == 1;
}

isOdd(9); // true
isOdd(6); // false

const isOdd2 = function(num) {
  // uhhh
}
